{
    "name": "a-normal-testing-repo",
    "version": "0.0.0-development",
    "description": "This is a repo for testing purpose only. Sometimes I just need to `git push` the changes for the stuff I'm playing around with. E.g., I need to have a sense about how `git push --tag` works. This repo is just for those kind of stuff.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\"",
        "semantic-release": "semantic-release pre && npm publish && semantic-release post"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/coolgod/a-normal-testing-repo.git"
    },
    "author": "coolgod",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/coolgod/a-normal-testing-repo/issues"
    },
    "homepage": "https://github.com/coolgod/a-normal-testing-repo#readme",
    "devDependencies": {
        "semantic-release": "^8.0.3"
    },
    "config": {
        "loglevel": "verbose"
    }
}
